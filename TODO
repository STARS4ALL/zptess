SHORT TERM
==========

* bugs
 Hay un lio con el nombre y el ZP en el stats service, cuando se activa prinero stars3 y lueho stars488
 Se mezcaln los nomnbres y el zp. talvez tiene que ver con el flag alone, que se envía a True en ambos casos

YA ESTOY DESHACEINDO EL LIO EN LA RAMA fix/zpmess
Ahora los ZP se calculan o bien con el ZP en el dispositivo o bien con un ficticio común 
que se lee de la BBDD en la seccion de calibración. 
En el modo command line, cuando se da --read, se usa el individual de cada dispositivo

Hay que ir eliminando el rastro de zp_abs (puede que en la BD no, lo mismo tengo que hacer una migracion ...)

* Tabla config_t
 - Borrar propiedad (ref-device, zp_abs)
 - (ref-device, zp) = 20.44
 - Insertar propiedad (calibration, zp_fict) = 20.50

* GUI

- DAO de ssioness de batch_t
  y el controlador que tenga openSession(), closeSession() y PurgeSession()


* Implementar en el GUI habliltar y desabilitar TCP/UDP opciones cuando
el modelo de TESSW es TAS o TESS-P. para loas ttk hay que hacerlo a través de los estilos
poque no tiene la opcion de 'state'

* Pending bug fixes:
  zptool.utils.paging() no funciona en estos caso:
  - con lsitas/tuplas y size=None
  - con cursores y size != None


* stars488 saca por el puerto serie solo el formato antiguo
* stars63 saca por el puerto serie tanto JSON como el formato antiguo
